# Raw User Tasks

Use this file to drop task ideas in your own language.

## How To Use

- Write in any language.
- Short notes are fine.
- One line or one small paragraph per idea is enough.
- The agent should read this file, combine related items into one structured task file when practical, and then track
  the structured task in `.ai/tasks/registry.md`.

## Existing Raw Notes

### 2026-04-01

- The printer is running using Python scripts.
- I have added those files in to .ai/resources/printer folder.
- Normally I run ` python .\printer-run.py` on Windows terminal.
- The issue is I have to run this file on the Windows terminal manually.
- Now I need to run that Printer script when starting this application automatically.
- In both dev and prod environments, the printer should run automatically.
- I can guarantee that the system running this application has Python installed.
- Now printer folders outside the application folder.
- If you think it should be present in the application folder, then I can move it to the application folder.
