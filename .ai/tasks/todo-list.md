# TODO list

- When changing the date, the doctor’s list should be updated since doctors are assigned to a date.
- "Date of birth / Age" both input fields together should take the same width as telephone or name takes. inside that
  width, date should take 75% of the width and age should take 25% of the width.
- We have to add a menu in between top and bottom area like tabs. refer to this screenshot's yellow lines
  .ai/resources/screenshot-03-30.png.
  - The first menu contains the current content. Add a good name for it.
  - The second menu contains the booking list
    - Please check the route list. It may have a route to take bookings for a selected date. By default, the date is
      today.
    - If not found the route, then add a route to take bookings for a selected date.
    - The booking list is more like a table. It has three actions:
      - Edit
      - Delete
      - Proceed to payment: once the booking is confirmed, it will create a bill and removed from the booking list.
