# Instruction Sync Task

## Goal

Keep local agent guidance in sync with the redesigned homepage and updated working practices.

## Scope

- Update `AGENTS.md` if the startup routing should change.
- Update `.ai/project-context.md` with the new homepage structure.
- Update `.ai/task-skills.md` and `.ai/workflow-rules.md` if new reusable UI conventions are introduced.
- Record any temporary gaps between UI and backend/domain persistence.
